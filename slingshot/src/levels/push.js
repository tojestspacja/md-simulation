// Level: the push. No bodies at all — the only thing to learn is the drag.
export default {
  id: "push", name: "the push", hint: "pull back from the probe, then let go",
  start: [120, 400], flag: [830, 400], flagR: 30, bodies: [], maxP: 300, maxT: 6,
};
